# 🚀 Деплой на Hetzner VPS (Рекомендация для ЕС)

## Почему Hetzner идеален для вас:

- 🇪🇺 **Серверы в Европе** (Германия ~400км от Праги)
- 💰 **€4.51/мес** (~110 Kč) - один из самых дешевых в ЕС
- ⚡ **Отличная скорость** для работы в Европе
- 🔒 **Надежность** - один из лучших хостеров
- 📊 **GDPR** - соответствие европейским законам

---

## Шаг 1: Создание сервера на Hetzner

### 1.1 Регистрация

1. Перейдите на https://www.hetzner.com
2. Нажмите **Sign Up** (в правом верхнем углу)
3. Заполните данные:
   - Email
   - Пароль
   - Адрес (можно указать адрес в Праге)
4. Подтвердите email

### 1.2 Добавление платежного метода

1. Войдите в аккаунт
2. Перейдите в **Billing** → **Payment Methods**
3. Добавьте карту (принимают карты из Чехии)

### 1.3 Создание проекта

1. В консоли Hetzner Cloud нажмите **New Project**
2. Название: `telegram-bot`
3. Нажмите на проект

### 1.4 Создание сервера (VPS)

1. Нажмите **Add Server**
2. **Location:** Falkenstein (Germany) - ближайший к Праге
3. **Image:** Ubuntu 22.04
4. **Type:** 
   - Shared vCPU
   - **CX22** (рекомендую)
     - 2 vCPU
     - 4 GB RAM
     - 40 GB SSD
     - €4.51/мес
5. **SSH Keys:** (пока пропустите, используем пароль)
6. **Name:** `telegram-bot-server`
7. Нажмите **Create & Buy now**

### 1.5 Получение доступа

После создания вы получите:
- IP-адрес сервера (например: `123.45.67.89`)
- Root пароль (в email или в консоли)

**Сохраните эти данные!**

---

## Шаг 2: Подключение к серверу

### Windows (PowerShell или Git Bash):

```bash
ssh root@ВАШ_IP_АДРЕС
```

Введите пароль который вам прислали.

При первом подключении появится вопрос:
```
Are you sure you want to continue connecting (yes/no)?
```
Напишите `yes` и нажмите Enter.

---

## Шаг 3: Настройка сервера

Скопируйте и выполните эти команды **по порядку**:

### 3.1 Обновление системы

```bash
apt update && apt upgrade -y
```

### 3.2 Установка Python и зависимостей

```bash
apt install python3 python3-pip git nano -y
```

### 3.3 Клонирование вашего репозитория

```bash
cd /opt
git clone https://github.com/vladyslavovsianko/telegram-bot.git
cd telegram-bot
```

### 3.4 Установка Python пакетов

```bash
pip3 install -r requirements.txt
```

### 3.5 Создание файла с переменными окружения

```bash
nano .env
```

**Скопируйте это и вставьте в nano:**

```env
# Токен бота от @BotFather
BOT_TOKEN=ваш_токен_бота

# Telegram API (получить на https://my.telegram.org)
API_ID=ваш_api_id
API_HASH=ваш_api_hash

# ID менеджеров (через запятую)
MANAGER_IDS=123456789,987654321

# ID модераторов (через запятую)
STATUS_MODERATORS=111111111,222222222

# ID канала витрины
TARGET_CHANNEL_ID=-1001234567890

# ID VIP группы
VIP_GROUP_ID=-1009876543210

# ID чата по умолчанию
TARGET_CHAT_ID=-1009876543210

# Задержка публикации в канал (секунды)
CHANNEL_POST_DELAY=10
```

**Замените все значения на настоящие!**

**Сохранение:**
- Нажмите `Ctrl+X`
- Нажмите `Y`
- Нажмите `Enter`

---

## Шаг 4: Настройка автозапуска (systemd)

### 4.1 Создание service файла

```bash
nano /etc/systemd/system/telegram-bot.service
```

### 4.2 Вставьте этот код:

```ini
[Unit]
Description=Telegram Bot for Munich Watches
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/telegram-bot
ExecStart=/usr/bin/python3 /opt/telegram-bot/bot_full.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Сохраните:** `Ctrl+X`, `Y`, `Enter`

### 4.3 Запуск бота

```bash
# Обновить systemd
systemctl daemon-reload

# Включить автозапуск при перезагрузке сервера
systemctl enable telegram-bot

# Запустить бота
systemctl start telegram-bot

# Проверить статус
systemctl status telegram-bot
```

Должно быть: `Active: active (running)`

---

## Шаг 5: Управление ботом

### Основные команды:

```bash
# Проверить статус
systemctl status telegram-bot

# Остановить бота
systemctl stop telegram-bot

# Запустить бота
systemctl start telegram-bot

# Перезапустить бота
systemctl restart telegram-bot

# Посмотреть логи (последние 50 строк)
journalctl -u telegram-bot -n 50

# Следить за логами в реальном времени
journalctl -u telegram-bot -f

# Выйти из просмотра логов: Ctrl+C
```

### Обновление бота после изменений:

```bash
# Подключитесь к серверу
ssh root@ВАШ_IP

# Перейдите в папку
cd /opt/telegram-bot

# Скачайте обновления из GitHub
git pull

# Перезапустите бота
systemctl restart telegram-bot

# Проверьте логи
journalctl -u telegram-bot -n 20
```

---

## Шаг 6: Мониторинг и безопасность

### Проверка использования ресурсов:

```bash
# Память и CPU
htop

# Выход: Q

# Проверка места на диске
df -h

# Размер папки бота
du -sh /opt/telegram-bot
```

### Настройка файрвола (безопасность):

```bash
# Установка UFW
apt install ufw -y

# Разрешить SSH (ВАЖНО!)
ufw allow 22/tcp

# Включить файрвол
ufw enable

# Проверить статус
ufw status
```

---

## ⚠️ Важные моменты:

### 1. **Резервное копирование базы данных**

```bash
# Создать бэкап
cp /opt/telegram-bot/bot_database.db /opt/telegram-bot/backup_$(date +%Y%m%d).db

# Скачать на компьютер
scp root@ВАШ_IP:/opt/telegram-bot/bot_database.db ./backup.db
```

### 2. **Мониторинг работы**

Бот автоматически перезапустится если упадет (настройка `Restart=always`)

### 3. **Логи**

Логи хранятся в systemd journal и автоматически ротируются

---

## 🎯 Что влияет на стабильность:

### ✅ ЧТО МЫ УЛУЧШИЛИ:

1. **Защита от утечки памяти** - кэш автоматически очищается
2. **Глобальный обработчик ошибок** - бот не упадет при ошибке
3. **Автоматический перезапуск** - systemd перезапустит бот если он упал
4. **Логирование** - все ошибки записываются в журнал
5. **Европейский сервер** - низкая задержка для работы в ЕС

### 📊 Факторы стабильности:

| Фактор | Влияние | Решение |
|--------|---------|---------|
| RAM | ⚠️ Средний | 4GB достаточно, кэш очищается |
| CPU | ✅ Низкий | 2 vCPU более чем достаточно |
| Сеть | ✅ Низкий | Hetzner - отличные каналы |
| База данных | ✅ Низкий | SQLite легкая |
| Ошибки в коде | ✅ Защита | Глобальный обработчик |

---

## 💰 Стоимость

- **Сервер CX22:** €4.51/мес (~110 Kč)
- **Трафик:** Неограниченный (20TB включено)
- **Бэкапы** (опционально): €0.90/мес

**Итого:** ~110-140 Kč в месяц

---

## 📞 Поддержка

- **Документация Hetzner:** https://docs.hetzner.com
- **Поддержка:** support@hetzner.com (отвечают на английском/немецком)

---

## 🎉 Готово!

После настройки бот будет работать 24/7 и автоматически перезапускаться при любых проблемах.

**Следующие шаги:**
1. Протестируйте бота
2. Настройте регулярные бэкапы базы данных
3. Мониторьте логи первые несколько дней
