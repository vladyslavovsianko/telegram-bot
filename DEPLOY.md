# 🚀 Инструкции по деплою бота

## Вариант 1: Railway.app (Самый простой)

### Шаги:

1. **Зарегистрируйтесь на Railway**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `telegram-bot`

3. **Настройте переменные окружения**
   - В проекте откройте "Variables"
   - Добавьте все переменные из `.env.example`:
     - `BOT_TOKEN`
     - `API_ID`
     - `API_HASH`
     - `MANAGER_IDS`
     - `STATUS_MODERATORS`
     - `TARGET_CHANNEL_ID`
     - `VIP_GROUP_ID`
     - `TARGET_CHAT_ID`
     - `CHANNEL_POST_DELAY`

4. **Деплой**
   - Railway автоматически задеплоит бота
   - Проверьте логи

**Стоимость:** ~$5/месяц после бесплатных кредитов

---

## Вариант 2: VPS (Ubuntu/Debian)

### 1. Подключитесь к серверу

```bash
ssh root@ваш-сервер-ip
```

### 2. Установите Python и зависимости

```bash
# Обновите систему
apt update && apt upgrade -y

# Установите Python и git
apt install python3 python3-pip git -y
```

### 3. Склонируйте репозиторий

```bash
cd /opt
git clone https://github.com/vladyslavovsianko/telegram-bot.git
cd telegram-bot
```

### 4. Установите зависимости

```bash
pip3 install -r requirements.txt
```

### 5. Создайте файл с переменными окружения

```bash
nano .env
```

Скопируйте содержимое из `.env.example` и заполните реальными значениями.
Сохраните: `Ctrl+X`, `Y`, `Enter`

### 6. Создайте systemd service для автозапуска

```bash
nano /etc/systemd/system/telegram-bot.service
```

Вставьте:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/telegram-bot
ExecStart=/usr/bin/python3 /opt/telegram-bot/bot_full.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl+X`, `Y`, `Enter`

### 7. Запустите бота

```bash
# Перезагрузите systemd
systemctl daemon-reload

# Включите автозапуск
systemctl enable telegram-bot

# Запустите бота
systemctl start telegram-bot

# Проверьте статус
systemctl status telegram-bot
```

### 8. Управление ботом

```bash
# Остановить бота
systemctl stop telegram-bot

# Перезапустить бота
systemctl restart telegram-bot

# Посмотреть логи
journalctl -u telegram-bot -f

# Обновить бота
cd /opt/telegram-bot
git pull
systemctl restart telegram-bot
```

**Стоимость:** От 200₽/месяц (Timeweb/Vdsina)

---

## Вариант 3: DigitalOcean (VPS с бонусом)

1. **Получите бонус $200**
   - Перейдите на https://m.do.co/c/your-referral-link
   - Зарегистрируйтесь (нужна карта)
   - Получите $200 на 60 дней бесплатно

2. **Создайте Droplet**
   - Выберите Ubuntu 22.04
   - План: Basic ($6/мес)
   - Регион: ближайший к вам

3. **Следуйте инструкциям из "Вариант 2: VPS"**

**Стоимость:** $6/месяц (первые 60 дней бесплатно с бонусом)

---

## Рекомендация

- **Новичок**: Railway.app (деплой за 5 минут)
- **Опытный**: VPS на Timeweb/Vdsina (дешевле, больше контроля)
- **С бонусом**: DigitalOcean (60 дней бесплатно)

---

## Автоматическое обновление из GitHub

После любого изменения кода:

```bash
# Локально
git add .
git commit -m "Описание изменений"
git push

# На сервере (VPS)
cd /opt/telegram-bot
git pull
systemctl restart telegram-bot

# Railway деплоится автоматически!
```
